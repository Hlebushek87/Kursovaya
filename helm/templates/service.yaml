apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: {{ .Values.namespace | default "prod" }}
spec:
  selector:
    app: app
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
